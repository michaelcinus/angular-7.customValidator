import { CodIsanValidatorDirective } from './cod-isan-validator.directive';

describe('CodIsanValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CodIsanValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
