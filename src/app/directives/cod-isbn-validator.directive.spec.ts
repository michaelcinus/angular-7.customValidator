import { CodIsbnValidatorDirective } from './cod-isbn-validator.directive';

describe('CodIsbnValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CodIsbnValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
